spring.application.name=api-cobrancas

spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/db_cobrancas}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:master}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
